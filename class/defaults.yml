---
parameters:
  renovate:
    =_metadata:
      multi_instance: true
      multi_tenant: true
    namespace: syn-${_instance}
    release_name: ${_instance}
    images:
      renovate:
        registry: ghcr.io
        repository: renovatebot/renovate
        tag: 43.58.0
      valkey:
        registry: docker.io
        repository: valkey/valkey
        tag: 9.0.3
    charts:
      renovate:
        source: oci://ghcr.io/renovatebot/charts/renovate
        version: 46.55.0
      valkey:
        source: https://valkey.io/valkey-helm
        version: 0.9.3
    components:
      valkey:
        enabled: false
        namespace: ${renovate:namespace}
        release_name: ${_instance}-valkey
    helm_values:
      image: ${renovate:images:renovate}
    valkey_helm_values:
      image: ${renovate:images:valkey}

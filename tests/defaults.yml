---
# Overwrite parameters here

parameters:
  renovate:
    charts:
      renovate:
        version: 46.168.0
    helm_values:
      cronjob:
        suspend: true
      renovate:
        config: |
          {
            "$schema": "https://docs.renovatebot.com/renovate-schema.json",
            "dryRun": "full"
          }
